package ui.custom;

import java.awt.*;

/**
 * DefaultPalette is a class that's only purpose is to
 * store the basic font colors for certain buttons and
 * text in the UI
 * */
public class DefaultPalette {
    public static final Color defaultColor = new Color(0x0277BD);
    public static final Color highlightColor = new Color(0x4FC3F7);
}
